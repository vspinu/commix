language: clojure
lein: 2.7.1
sudo: false
cache:
  directories:
    - $HOME/.m2
script:
  - $CMD
env:
  - CMD="lein with-profile 1.7 test-all"
  - CMD="lein with-profile 1.9 do test, test-node"
  - CMD="lein with-profile 1.9-no-spec do test, test-node"
jdk:
  - openjdk7
  - oraclejdk8
